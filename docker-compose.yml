version: '3.8'

services:
  dinomath:
    build:
      context: .
      dockerfile: Dockerfile
    restart: always
    ports:
      - "80:80"
    environment:
      - PORT=80
      - NODE_ENV=production
      - DATABASE_DIR=/app/data
      - VITE_ELEVENLABS_API_KEY=sk_7459c04760770cce19a02a605edbd60102ac07908f85fa08
    volumes:
      - dinomath_data:/app/data

volumes:
  dinomath_data:
